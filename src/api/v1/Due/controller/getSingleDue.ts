import { RequestHandler } from "express";
import asyncHandeler from "../../../../utils/asyncHandeler";

const getSingleDue:RequestHandler = asyncHandeler((req,res,next)=>{
  const dueId = req.params.id;








})



export default getSingleDue